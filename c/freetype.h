#include "/usr/include/limits.h"

#define FT_ERRORDEF( e, v, s )
#define FT_ENC_TAG( value, a, b, c, d ) value = 0

#include <ft2build.h>
#include FT_FREETYPE_H
#include FT_GLYPH_H
#include FT_OUTLINE_H
